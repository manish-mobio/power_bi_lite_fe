# Backend API URL for Power BI Lite (schema, query, dashboards)
# Example: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Path segment after origin for REST API (no leading slash required)
# Example: api/v1
NEXT_PUBLIC_API_VERSION=api/v1
